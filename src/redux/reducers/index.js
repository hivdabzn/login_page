import { combineReducers } from "redux";

// Buraya kendi reducer'larını ekleyebilirsin, şimdilik boş bir reducer ekliyoruz.
const rootReducer = combineReducers({
  // auth: authReducer,  // Eğer bir auth reducer'in varsa buraya ekle
});

export default rootReducer;
